---
spring:
    application.name: ${APPLICATION_NAME:this-one-has-no-name}
    cloud.config.uri: ${CONFIG_SERVER_URI:http://configuration-server:8888}
    profiles.active: ${CONFIG_PROFILE:development}
...
